use std::fs::File;
use std::io::prelude::*;

fn main() -> std::io::Result<()> {
    
    Ok(())
}

#[cfg(test)]
mod tests {

    #[test]
    fn it_works_for_example_1() {
        assert_eq!(2, 2);
    }

    
}
